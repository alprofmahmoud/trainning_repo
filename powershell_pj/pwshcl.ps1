gpedit.msc					# Local Group Polies
calc						# Calculator
logoff						# Logoff
shutdown -s -t 0				# Shutdown now // -t = time second 
[Environment]::GetEnvironmentVariable('nam_var')# get value variable
[environment]::SetEnvironmentVariable(var_name,value_var,scope)# Set variable in system
ipconfig /displaydns				# Show Cache DNS in client
ipconfig /flushdns				# Clear cache DNS
nslookup					# Ask DNS
mklink /H or /D or /J link target		# make soft link to file /H hardlink, /D dirctory, /J jounjaction 
 get-
get
